# RNN-Recurrent-Neural-Network-
Developed a Text Classification model using RNN to automatically categorize text into different classes. The model learns patterns from text data and improves the accuracy of automated text analysis.
